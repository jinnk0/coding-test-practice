# [문제 번호] 문제 이름

## 🔗 문제 링크
- (여기에 문제 주소 붙여넣기)

## 💡 핵심 로직 (Approach)
- **알고리즘:** (예: BFS, 그리디, 이분 탐색 등)
- **풀이 전략:** - (예: 무게가 가벼운 순서대로 정렬한 뒤 그리디하게 선택)
  - (예: 방문 처리를 위해 2차원 배열 사용)

## ❌ 삽질 포인트 (Trial & Error)
- **실수했던 부분:** (예: 소수점 처리 문제, 시간 복잡도 O(N²)으로 초과 발생)
- **해결 방안:** (예: math.floor 대신 // 사용, 딕셔너리를 사용해 O(N)으로 단축)

## ✍️ 언어별 차이 및 복습
- **Python:** `sort()` 하나면 해결되는데 코드가 매우 간결함.
- **Java:** `PriorityQueue` 사용 시 `Comparator`를 직접 정의해줘야 함. (문법 다시 체크!)
